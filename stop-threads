#!/data/data/com.termux/files/usr/bin/env bash
# stop-threads: Stop CodeAir Threads + Instagram Agent daemon

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$SCRIPT_DIR/scripts/stop-agent.sh" "$@"
