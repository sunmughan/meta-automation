#!/data/data/com.termux/files/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "\${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR" || exit 1
pkill -f "node .*job-agent.js" 2>/dev/null || true
echo "Job revenue engine stopped."
